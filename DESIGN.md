# 方案：原版 Omarchy 输入框增加中文输入

已按此方案实现并部署至 lj-t480s；真实中文预编辑、选词、原命令提交与取消已验证。

## 做法

使用 Omarchy 官方插件克隆机制，在原版菜单代码上做局部修改，不另写一个输入框应用。

1. **保留原界面和主题。** 原来的窗口、卡片、遮罩、尺寸、边框、字体、颜色全部不动，继续使用宿主的 `Color`、`Style` 和 UI 组件，因此仍跟随系统主题。
2. **只替换 input 模式的文字控件。** 将只读 `Text` 换为无背景、无额外 padding 的 `QtQuick.TextInput`，沿用原位置、字体、颜色和提示文字。普通菜单与 select 模式不改。
3. **把焦点交给 TextInput。** input 模式停用原 `keyCatcher` 的字符拼接逻辑，由 Qt 和系统现有 Fcitx5 处理中文、预编辑、候选词、粘贴和光标编辑。不新增输入法配置。
4. **保留提交接口。** 输入完成后将 `TextInput.text` 交给原 `applyDmenuSelection()`。选词时的 Enter/Escape 先交给输入法，不能同时触发提交或关闭。

## 复用与调用

通过官方 `omarchy plugin clone omarchy.menu` 承载补丁，不修改系统文件。克隆机制会将原菜单调用路由到个人版本，调用方式保持：

```bash
text=$(omarchy menu input "Ask Pi" --width 650) || exit
```

主要改动集中在克隆版 `Menu.qml`：文字控件、焦点分流和按键处理。Bash 入口、IPC、返回值、主题系统均复用原版，不增加服务、框架或通用组件。

外观只新增真实编辑必需的光标、选区和预编辑显示；不增加表单边框或按钮。

## 完成标准

看起来仍是原版输入框；能正常用输入法输入中文；切换系统主题仍一致；原命令能返回输入结果。无需扩展成全面审计或重构任务。
